import React from "react";
import { Card, CardTitle, CardBadge } from "checklist-kitchen-ds";

export function Default() {
    return (
        <Card variant="list-item">
            <CardTitle>
                SC2 - Fridge/Freezer Temperatures <CardBadge>Required</CardBadge>
            </CardTitle>
        </Card>
    );
}
