import React from "react";
import { Card, CardTitle, CardArrow } from "checklist-kitchen-ds";

export function Default() {
    return (
        <Card variant="list-item">
            <CardTitle>SC4 - Hot/Cold Holding</CardTitle>
            <CardArrow />
        </Card>
    );
}
